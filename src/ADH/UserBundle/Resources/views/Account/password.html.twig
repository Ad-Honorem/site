{% extends "ADHSkeletonBundle::base.html.twig" %}

{% block content %}
	{{ form_start(form, {"action": path("adh_user_account_password")}) }}
		{{ form_errors(form) }}
		
		{# old password #}
		{{ form_row(form.old) }}
		
		{# new password #}
		{{ form_row(form.new) }}
		
		{# submit #}
		{{ form_row(form.submit) }}
	{{ form_end(form) }}
{% endblock content %}