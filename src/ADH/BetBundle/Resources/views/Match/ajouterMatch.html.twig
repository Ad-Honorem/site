{% extends "::base.html.twig" %}

{% block content %}
    <h3>Ajout d'un match</h3>
    <hr>
    <div class="row">
        {{ form_start(form, { 'attr' : {'class' : 'form-horizontal'}}) }}
        {{ form_errors(form) }}
        <div class="form-group">
            {{ form_label(form.equipeA, "Equipe A : ",{'attr' : {'class' : 'col-sm-2 control-label'}}) }}
            <div class="col-sm-4">
                {{ form_widget(form.equipeA) }}
            </div>
            {{ form_label(form.equipeB, "Equipe B : ",{'attr' : {'class' : 'col-sm-2 control-label'}}) }}
            <div class="col-sm-4">
                {{ form_widget(form.equipeB) }}
            </div>
        </div>
        <div class="form-group">
            {{ form_label(form.type, "Type :",{'attr' : {'class' : 'col-sm-2 control-label'}}) }}
            <div class="col-sm-4">
                {{ form_widget(form.type, { 'attr': {'class': 'form-control'} }) }}
            </div>
            {{ form_label(form.ladate) }}
            <div class="col-sm-4">
                {{ form_widget(form.ladate) }}
            </div>
        </div>
            <br />
        <div class="form-group">
            <div class="col-sm-offset-1 col-sm-4">
                <input type="submit" value="Créer" class="btn btn-info form-control" />
            </div>
            <div class="col-sm-offset-1 col-sm-4">
                <input type="reset" value="Annuler" class="btn btn-danger form-control" onclick='location.href = "{{ path('adh_bet_match') }}"' />
            </div>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock content %}